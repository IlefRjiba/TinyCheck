import { JoinNamesPipe } from './join-names.pipe';

describe('JoinNamesPipe', () => {
  it('create an instance', () => {
    const pipe = new JoinNamesPipe();
    expect(pipe).toBeTruthy();
  });
});
